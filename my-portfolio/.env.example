# Copy to .env.local for local dev, or set as a build-time variable in CI.
# Value comes from the SAM stack output "ContactApiUrl".
#   sam deploy --profile personal   # prints ContactApiUrl
REACT_APP_CONTACT_API_URL=https://xxxxxxxxxx.execute-api.us-east-1.amazonaws.com/contact
